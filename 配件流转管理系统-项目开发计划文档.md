# 配件流转管理系统 — 项目开发计划文档

> **文档版本**：V1.0  
> **编写日期**：2026-05-27  
> **文档性质**：内部自用项目开发计划  
> **适用范围**：配件流转管理系统（PC + H5 双端）

---

## 目录

1. [项目概述](#1-项目概述)
2. [需求分析](#2-需求分析)
3. [系统功能模块设计](#3-系统功能模块设计)
4. [技术架构设计](#4-技术架构设计)
5. [开发环境与技术栈](#5-开发环境与技术栈)
6. [任务分工](#6-任务分工)
7. [开发排期计划](#7-开发排期计划)
8. [测试方案](#8-测试方案)
9. [打包部署流程](#9-打包部署流程)
10. [上线验收标准](#10-上线验收标准)
11. [使用与运维说明](#11-使用与运维说明)

---

## 1. 项目概述

### 1.1 项目背景

当前公司使用商用软件管理机器工件/配件的入库、领用、归还、售卖等流转环节，存在以下核心痛点：

- **操作繁琐**：无法一步扫码完成出入库，流程链路过长；
- **人员管理复杂**：需为每位师傅创建子账号，存在账号冲突与权限混乱问题；
- **追溯效率低**：无法直观查看单个师傅的整体库存，按条码追溯配件流向操作困难。

为解决上述问题，本项目将自建一套**配件流转管理系统**，以**条码扫码**为核心交互方式，覆盖配件从入库到出库的全生命周期，实现库存可视化与流转全追溯。

### 1.2 项目目标

| 维度 | 目标描述 |
|------|---------|
| 功能覆盖 | 完整实现配件入库、领用、归还、售卖、人员管理、查询追溯等全部业务功能 |
| 交互效率 | 扫码一步完成核心操作，单笔流转操作耗时 ≤ 5 秒 |
| 双端互通 | PC 网页端 + 手机 H5 端数据实时同步，业务逻辑一致 |
| 数据安全 | 流转记录持久化存储，不可随意删除，全操作留痕 |
| 部署友好 | 兼容 Windows / Linux 服务器，提供完整部署文档与运维指南 |

### 1.3 项目范围

- **包含**：后端 API 服务、PC 前端、H5 前端、数据库设计、部署脚本、使用手册、运维文档。
- **不包含**：多租户支持、第三方系统对接、移动端原生 APP（仅 H5）、复杂权限角色体系。

### 1.4 项目干系人

| 角色 | 职责 |
|------|------|
| 项目负责人 / 全栈开发 | 全程负责需求确认、编码开发、接口联调、功能自测、打包部署、上线运维 |
| 系统管理员（用户代表） | 需求确认、验收测试、日常使用 |
| 作业师傅 | 流转对象，系统中仅记录姓名，无登录权限 |
| 外部客户 | 售卖出库时选填关联，无需登录 |

---

## 2. 需求分析

### 2.1 用户角色与权限矩阵

| 功能 | 系统管理员 | 作业师傅 | 外部客户 |
|------|:---------:|:-------:|:-------:|
| 登录系统 | ✅ | ❌ | ❌ |
| 配件入库 | ✅ | ❌ | ❌ |
| 配件领用 | ✅ | ❌ | ❌ |
| 配件归还 | ✅ | ❌ | ❌ |
| 配件售卖 | ✅ | ❌ | ❌ |
| 师傅管理（增删改） | ✅ | ❌ | ❌ |
| 库存查询 | ✅ | ❌ | ❌ |
| 流转追溯查询 | ✅ | ❌ | ❌ |
| 操作日志查看 | ✅ | ❌ | ❌ |
| 配件分类管理 | ✅ | ❌ | ❌ |

> **说明**：本系统为单管理员模式，作业师傅仅作为流转对象被记录（姓名），外部客户信息在售卖时选填，均无系统登录权限。

### 2.2 核心业务流程

#### 2.2.1 配件入库流程

```
管理员扫码/手动输入条码
       ↓
  系统查询条码是否已存在
       ↓
  ┌─ 不存在：录入配件信息（名称、规格、分类等）→ 写入总库存
  └─ 已存在：提示已入库，可选择更新信息
       ↓
  生成入库操作记录
```

#### 2.2.2 配件领用流程

```
管理员扫码（支持逐个 / 批量）
       ↓
  系统加载配件信息，确认库存充足
       ↓
  选择领用师傅
       ↓
  配件从总库存转移至师傅名下库存
  （总库存数量 -1，师傅库存数量 +1）
       ↓
  生成领用操作记录
```

#### 2.2.3 配件归还流程

```
管理员扫码（支持逐个 / 批量）
       ↓
  系统加载配件信息，确认该师傅名下存在此配件
       ↓
  选择归还师傅（或系统自动匹配）
       ↓
  配件从师傅库存回流至总库存
  （师傅库存数量 -1，总库存数量 +1）
       ↓
  生成归还操作记录
```

#### 2.2.4 配件售卖流程

```
管理员扫码
       ↓
  系统加载配件信息，确认总库存充足
       ↓
  填写售卖信息（可选填写客户名称、联系方式等）
       ↓
  配件从总库存扣减
       ↓
  生成售卖出库记录
```

### 2.3 功能需求清单

| 编号 | 功能模块 | 功能点 | 优先级 |
|------|---------|--------|:------:|
| F-001 | 用户认证 | 管理员账号登录、退出、Token 鉴权 | P0 |
| F-002 | 配件入库 | 扫码/手动录入条码，录入配件基本信息，写入库存 | P0 |
| F-003 | 配件领用 | 扫码选择师傅，转移库存，支持批量操作 | P0 |
| F-004 | 配件归还 | 扫码选择师傅，回流库存，支持批量操作 | P0 |
| F-005 | 配件售卖 | 扫码扣减库存，选填客户信息，生成出库记录 | P0 |
| F-006 | 师傅管理 | 师傅信息 CRUD（姓名、工号、联系方式等） | P0 |
| F-007 | 配件分类 | 配件品类分类管理（增删改） | P0 |
| F-008 | 总库存查询 | 查看全部配件库存，支持按分类筛选、关键词搜索 | P0 |
| F-009 | 师傅库存查询 | 按师傅查看名下全部配件及领用记录 | P0 |
| F-010 | 条码追溯 | 按条码查询配件全流程流转记录 | P0 |
| F-011 | 操作日志 | 全操作日志留存，支持多条件筛选（时间、类型、操作人） | P1 |
| F-012 | H5 扫码 | 手机摄像头扫描一维条码，调用设备摄像头 | P0 |
| F-013 | PC 端适配 | 主流浏览器兼容（Chrome、Edge、Firefox） | P0 |
| F-014 | H5 端适配 | 移动端浏览器适配，响应式布局 | P0 |
| F-015 | 数据实时同步 | PC 与 H5 双端操作后数据即时一致 | P0 |

### 2.4 非功能需求

| 维度 | 要求 |
|------|------|
| 性能 | 内部小范围使用，并发量低，单接口响应 < 1s |
| 兼容性 | PC：Chrome 90+、Edge 90+、Firefox 90+；H5：iOS Safari 14+、Android Chrome 90+ |
| 数据持久化 | MySQL 存储，流转记录不可物理删除，服务重启不丢数据 |
| 安全性 | 管理员登录鉴权，接口 Token 验证，防 SQL 注入、XSS |
| 可维护性 | 代码结构清晰，分层规范，注释充分，便于后续迭代 |
| 可部署性 | 兼容 Windows / Linux，配置文件外置可修改 |
| 可用性 | 操作流程简洁，扫码一步到位，核心操作 ≤ 3 步完成 |

---

## 3. 系统功能模块设计

### 3.1 系统模块总览

```
配件流转管理系统
├── 用户认证模块
│   ├── 管理员登录
│   ├── Token 签发与验证
│   └── 退出登录
├── 配件流转核心模块
│   ├── 配件入库
│   ├── 配件领用
│   ├── 配件归还
│   └── 配件售卖
├── 人员管理模块
│   └── 师傅信息 CRUD
├── 配件分类模块
│   └── 品类分类 CRUD
├── 库存管理模块
│   ├── 总库存视图
│   ├── 师傅库存视图
│   └── 库存统计
├── 查询追溯模块
│   ├── 配件条码追溯
│   ├── 师傅领用记录查询
│   └── 多条件组合查询
├── 操作日志模块
│   ├── 日志自动记录
│   └── 日志查询筛选
└── 前端展示模块
    ├── PC 端管理后台
    └── H5 移动端
        ├── 扫码操作页
        └── 移动端查询页
```

### 3.2 各模块详细设计

#### 3.2.1 用户认证模块

- 单管理员账号，系统初始化时预置。
- 登录成功后签发 JWT Token，前端存储于 `localStorage`。
- 所有业务接口通过拦截器校验 Token 有效性。
- Token 过期时间建议 24 小时，过期后需重新登录。

#### 3.2.2 配件流转核心模块

**配件入库**

- 支持扫码自动填入条码或手动输入条码。
- 系统校验条码唯一性：若已存在则提示，避免重复入库。
- 录入字段：条码（必填）、配件名称、规格型号、分类（下拉选择）、单位、备注。
- 入库成功后自动写入库存表和操作流水表。

**配件领用**

- 扫码加载配件信息，校验总库存 ≥ 1。
- 选择领用师傅（下拉搜索）。
- 支持**批量领用**：连续扫码多个配件，统一确认后批量提交。
- 库存变更事务：总库存扣减 + 师傅库存增加，原子操作。
- 生成领用流水记录。

**配件归还**

- 扫码加载配件信息，校验该师傅名下存在此配件。
- 选择归还师傅（或根据条码自动匹配当前持有师傅）。
- 支持**批量归还**。
- 库存变更事务：师傅库存扣减 + 总库存增加，原子操作。
- 生成归还流水记录。

**配件售卖**

- 扫码加载配件信息，校验总库存 ≥ 1。
- 可选填写客户信息：客户名称、联系电话。
- 总库存扣减。
- 生成售卖出库记录。

#### 3.2.3 人员管理模块

- 师傅信息字段：姓名（必填）、工号（选填）、联系电话（选填）、备注。
- 支持新增、编辑、列表查询、删除。
- 删除前校验：若师傅名下有未归还配件，提示不允许删除（或仅允许软删除/禁用）。

#### 3.2.4 配件分类模块

- 分类字段：分类名称、排序号、备注。
- 支持 CRUD 操作。
- 配件入库时下拉选择分类，库存查询时按分类筛选。

#### 3.2.5 库存管理模块

- **总库存视图**：展示全部配件库存，支持按分类筛选、关键词搜索（名称/条码），分页展示。
- **师傅库存视图**：选择具体师傅后，展示该师傅名下全部配件及领用时间。
- **库存统计**（可选扩展）：总配件数、总品类数、在库数、领用在途数、已售数。

#### 3.2.6 查询追溯模块

- **条码追溯**：输入或扫码条码，展示该配件从入库到当前状态的完整流转时间线。
- **师傅领用记录**：按师傅查询全部领用/归还记录，支持时间范围筛选。
- **多条件组合查询**：按时间范围、操作类型（入库/领用/归还/售卖）、师傅、条码、配件名称等组合筛选。

#### 3.2.7 操作日志模块

- 每次业务操作自动记录日志：操作时间、操作类型、操作内容、关联配件条码、关联人员。
- 日志仅可查询，不可修改、不可删除。
- 支持按时间范围、操作类型筛选。

### 3.3 数据库概念设计

#### 核心数据表

| 表名 | 说明 | 核心字段 |
|------|------|---------|
| `sys_user` | 管理员用户表 | id, username, password_hash, status, create_time |
| `worker` | 师傅信息表 | id, name, job_no, phone, remark, status, create_time |
| `category` | 配件分类表 | id, name, sort, remark, create_time |
| `accessory` | 配件信息表（主数据） | id, barcode, name, spec, category_id, unit, remark, create_time |
| `inventory` | 库存主表 | id, accessory_id, total_qty, available_qty, update_time |
| `inventory_owner` | 师傅库存表 | id, accessory_id, worker_id, qty, update_time |
| `flow_record` | 流转流水表 | id, accessory_id, barcode, flow_type, from_owner_type, from_owner_id, to_owner_type, to_owner_id, worker_id, customer_name, customer_phone, remark, operator, create_time |
| `operation_log` | 操作日志表 | id, action_type, content, related_barcode, related_worker_id, operator, create_time |

#### 流转类型枚举（`flow_type`）

| 值 | 含义 |
|----|------|
| `INBOUND` | 入库 |
| `TRANSFER_OUT` | 领用（总库 → 师傅） |
| `TRANSFER_IN` | 归还（师傅 → 总库） |
| `SELL` | 售卖出库 |

#### ER 关系概要

```
category  1──N  accessory
accessory 1──1  inventory
accessory 1──N  inventory_owner
accessory 1──N  flow_record
worker    1──N  inventory_owner
worker    1──N  flow_record
```

---

## 4. 技术架构设计

### 4.1 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        客户端层                              │
│  ┌──────────────────────┐    ┌──────────────────────┐       │
│  │   PC 管理后台 (Vue3)  │    │   H5 移动端 (Vue3)   │       │
│  │   Element Plus       │    │   Vant + QuaggaJS    │       │
│  │   Axios + Vue Router │    │   Axios + Vue Router │       │
│  └──────────┬───────────┘    └──────────┬───────────┘       │
│             │  HTTP / HTTPS             │                    │
└─────────────┼───────────────────────────┼────────────────────┘
              │                           │
┌─────────────┼───────────────────────────┼────────────────────┐
│             ▼           Nginx           ▼                    │
│  ┌──────────────────────────────────────────────────────┐   │
│  │                 反向代理 / 静态资源服务                  │   │
│  │         PC 端静态文件 + H5 端静态文件 + API 代理         │   │
│  └──────────────────────────┬───────────────────────────┘   │
│                              │                               │
│  ┌───────────────────────────▼──────────────────────────┐   │
│  │              Spring Boot 后端服务                      │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐             │   │
│  │  │Controller│→│ Service  │→│  Mapper  │             │   │
│  │  │  (REST)  │ │ (业务层) │ │ (数据层) │             │   │
│  │  └──────────┘ └──────────┘ └──────────┘             │   │
│  │  ┌──────────────────────────────────────┐           │   │
│  │  │ MyBatis-Plus  |  JWT  |  参数校验      │           │   │
│  │  └──────────────────────────────────────┘           │   │
│  └──────────────────────────┬───────────────────────────┘   │
│                              │                               │
│  ┌───────────────────────────▼──────────────────────────┐   │
│  │                   MySQL 数据库                        │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 后端架构分层

| 层级 | 职责 | 技术实现 |
|------|------|---------|
| Controller 层 | 接收请求、参数校验、返回响应 | Spring MVC + `@RestController` |
| Service 层 | 业务逻辑处理、事务管理 | Spring `@Service` + `@Transactional` |
| Mapper 层 | 数据库 CRUD 操作 | MyBatis-Plus `BaseMapper` |
| Entity 层 | 数据库实体映射 | Lombok + MyBatis-Plus 注解 |
| Common 层 | 统一响应、异常处理、工具类 | `@RestControllerAdvice` 全局异常 |

### 4.3 前端架构设计

#### PC 端

- Vue3 + Composition API
- Element Plus 组件库（表格、表单、对话框、分页等）
- Vue Router 路由管理
- Axios 请求封装（统一拦截、Token 注入、错误处理）
- Pinia 状态管理（可选，按需）

#### H5 端

- Vue3 + Composition API
- Vant 4 移动端组件库
- **QuaggaJS** 或 **Html5-QRCode** 实现摄像头一维码扫描
- Vue Router 路由管理
- Axios 请求封装
- 响应式布局（rem / vw 适配方案）

#### 双端共享策略

- 共用同一套后端 API，通过不同前端入口区分。
- API 接口统一 RESTful 风格，返回格式一致。
- H5 端与 PC 端可共用一个 Vue 工程（通过路由或构建配置区分），也可拆分为两个独立工程。

### 4.4 关键技术选型说明

| 技术 | 版本建议 | 选型理由 |
|------|---------|---------|
| JDK | 17 (LTS) | 长期支持，Spring Boot 3.x 要求 |
| Spring Boot | 3.2.x | 主流稳定版本，生态完善 |
| MyBatis-Plus | 3.5.x | 简化 CRUD，内置分页、代码生成 |
| MySQL | 8.0+ | 成熟稳定，JSON 类型支持 |
| Vue 3 | 3.4.x | Composition API，性能优化 |
| Element Plus | 2.5.x | PC 端最成熟的 Vue3 组件库 |
| Vant 4 | 4.x | 移动端 Vue 组件库，轻量好用 |
| QuaggaJS | 1.8.x | 纯前端一维条码识别，支持摄像头 |
| Nginx | 1.24+ | 反向代理、静态资源托管 |

### 4.5 接口设计规范

- RESTful 风格，路径小写，用 `-` 分隔。
- 统一响应格式：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": { }
}
```

- 分页请求统一格式：

```json
{
  "pageNum": 1,
  "pageSize": 20
}
```

- 分页响应统一格式：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "records": [],
    "total": 100,
    "pageNum": 1,
    "pageSize": 20
  }
}
```

### 4.6 核心 API 清单（概要）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/login` | 管理员登录 |
| POST | `/api/auth/logout` | 退出登录 |
| GET | `/api/auth/info` | 获取当前用户信息 |
| POST | `/api/accessories` | 配件入库（新增配件） |
| GET | `/api/accessories/{barcode}` | 根据条码查询配件信息 |
| PUT | `/api/accessories/{id}` | 更新配件信息 |
| GET | `/api/inventory` | 总库存查询（分页、筛选） |
| GET | `/api/inventory/worker/{workerId}` | 师傅库存查询 |
| POST | `/api/flow/transfer-out` | 配件领用（支持批量） |
| POST | `/api/flow/transfer-in` | 配件归还（支持批量） |
| POST | `/api/flow/sell` | 配件售卖 |
| GET | `/api/flow/trace/{barcode}` | 条码追溯（完整流转时间线） |
| GET | `/api/flow/records` | 流转记录查询（分页、筛选） |
| POST | `/api/workers` | 新增师傅 |
| PUT | `/api/workers/{id}` | 编辑师傅 |
| DELETE | `/api/workers/{id}` | 删除师傅 |
| GET | `/api/workers` | 师傅列表查询 |
| POST | `/api/categories` | 新增配件分类 |
| PUT | `/api/categories/{id}` | 编辑分类 |
| DELETE | `/api/categories/{id}` | 删除分类 |
| GET | `/api/categories` | 分类列表 |
| GET | `/api/logs` | 操作日志查询（分页、筛选） |

---

## 5. 开发环境与技术栈

### 5.1 开发环境

| 工具 | 版本 | 用途 |
|------|------|------|
| JDK | 17+ | 后端编译运行 |
| Maven | 3.9+ | 后端构建与依赖管理 |
| Node.js | 18+ / 20+ | 前端构建运行 |
| npm / pnpm | 最新稳定版 | 前端包管理 |
| MySQL | 8.0+ | 数据库 |
| IntelliJ IDEA | 最新版 | 后端开发 IDE |
| VS Code | 最新版 | 前端开发 IDE |
| Git | 最新版 | 版本控制 |
| Postman / Apifox | 最新版 | 接口调试 |
| Chrome DevTools | — | 前端调试、移动端模拟 |
| Navicat / DBeaver | — | 数据库管理工具 |

### 5.2 完整技术栈汇总

```
┌─────────────────────────────────────────────────┐
│                 技术栈全景                       │
├──────────────┬──────────────────────────────────┤
│  后端         │ Java 17 + Spring Boot 3.2       │
│              │ MyBatis-Plus 3.5                  │
│              │ JWT (jjwt)                        │
│              │ Lombok                            │
│              │ Hutool 工具库（可选）              │
├──────────────┼──────────────────────────────────┤
│  前端 PC      │ Vue 3 + Vite                    │
│              │ Element Plus                      │
│              │ Vue Router + Axios                │
│              │ Pinia（按需）                     │
├──────────────┼──────────────────────────────────┤
│  前端 H5      │ Vue 3 + Vite                    │
│              │ Vant 4                            │
│              │ QuaggaJS（一维码扫码）             │
│              │ Vue Router + Axios                │
├──────────────┼──────────────────────────────────┤
│  数据库       │ MySQL 8.0                        │
├──────────────┼──────────────────────────────────┤
│  部署         │ Nginx（反向代理 + 静态资源）      │
│              │ systemd / NSSM（服务自启）        │
│              │ JDK 17 + Maven（编译打包）        │
└──────────────┴──────────────────────────────────┘
```

---

## 6. 任务分工

### 6.1 角色分工

> 本项目为单人全栈开发，以下按模块维度梳理任务，用于自我管理和进度跟踪。

| 序号 | 任务模块 | 具体工作项 | 负责人 |
|:----:|---------|-----------|:------:|
| 1 | 项目初始化 | 后端 Spring Boot 工程搭建、前端 Vue 工程搭建（PC + H5）、数据库建表 | 本人 |
| 2 | 用户认证 | 管理员登录接口、JWT 鉴权拦截器、前端登录页、Token 管理 | 本人 |
| 3 | 师傅管理 | 后端 CRUD 接口、前端师傅管理页面（PC + H5） | 本人 |
| 4 | 配件分类 | 后端 CRUD 接口、前端分类管理页面 | 本人 |
| 5 | 配件入库 | 入库接口、条码校验逻辑、前端入库页面（含扫码） | 本人 |
| 6 | 库存管理 | 总库存查询接口、师傅库存查询接口、前端库存展示页面 | 本人 |
| 7 | 配件领用 | 领用接口（含批量事务）、前端领用页面（含扫码 + 选师傅） | 本人 |
| 8 | 配件归还 | 归还接口（含批量事务）、前端归还页面 | 本人 |
| 9 | 配件售卖 | 售卖接口、前端售卖页面（含客户信息选填） | 本人 |
| 10 | 查询追溯 | 条码追溯接口、师傅记录查询接口、前端追溯页面 | 本人 |
| 11 | 操作日志 | 日志记录机制、日志查询接口、前端日志页面 | 本人 |
| 12 | H5 扫码 | 摄像头调用、一维码识别集成、扫码交互优化 | 本人 |
| 13 | 双端联调 | PC 与 H5 数据一致性验证、接口联调 | 本人 |
| 14 | 测试 | 功能自测、边界场景测试、双端兼容性测试 | 本人 |
| 15 | 部署上线 | Nginx 配置、服务打包、部署脚本、自启动配置 | 本人 |
| 16 | 文档 | 部署文档、使用手册、运维说明 | 本人 |

---

## 7. 开发排期计划

> 以下排期基于单人全栈开发模式估算，总计约 **18 个工作日**。

### 7.1 阶段总览

| 阶段 | 时间 | 工作日 | 主要内容 |
|------|------|:------:|---------|
| Phase 1：基础搭建 | 第 1-3 天 | 3 | 工程初始化、数据库设计、认证模块、师傅管理、分类管理 |
| Phase 2：核心流转 | 第 4-8 天 | 5 | 入库、领用、归还、售卖全流程后端 + 前端 |
| Phase 3：查询与追溯 | 第 9-11 天 | 3 | 库存查询、条码追溯、师傅记录查询、操作日志 |
| Phase 4：H5 与扫码 | 第 12-14 天 | 3 | H5 端页面开发、摄像头扫码集成、响应式适配 |
| Phase 5：联调测试 | 第 15-16 天 | 2 | 双端联调、功能自测、Bug 修复、边界场景验证 |
| Phase 6：部署文档 | 第 17-18 天 | 2 | 部署打包、Nginx 配置、部署文档、使用手册、运维文档 |

### 7.2 详细排期

| 日期 | 天数 | 工作内容 | 产出物 |
|------|:----:|---------|--------|
| D1 | 1 | 后端 Spring Boot 工程搭建（目录结构、依赖、配置）；前端 Vue 工程初始化（PC + H5）；数据库表设计与 SQL 脚本编写 | 后端骨架工程、前端骨架工程、建表 SQL |
| D2 | 1 | 管理员登录接口（JWT 签发/验证）；全局异常处理、统一响应封装；前端登录页（PC）；Axios 拦截器封装 | 登录功能可用、Token 鉴权生效 |
| D3 | 1 | 师傅管理 CRUD（后端 + 前端页面）；配件分类 CRUD（后端 + 前端页面）；PC 端侧边栏布局与路由配置 | 师傅/分类管理页面可用 |
| D4 | 1 | 配件入库接口（条码校验、信息录入）；前端入库页面（含条码输入、信息表单） | 入库功能可用 |
| D5 | 1 | 总库存查询接口（分页、筛选）；师傅库存查询接口；前端库存列表页面（总库存视图 + 师傅库存视图） | 库存查询页面可用 |
| D6 | 1 | 配件领用接口（含批量事务处理）；前端领用页面（扫码 + 选择师傅 + 批量操作） | 领用功能可用 |
| D7 | 1 | 配件归还接口（含批量事务处理）；前端归还页面 | 归还功能可用 |
| D8 | 1 | 配件售卖接口（含客户信息选填）；前端售卖页面 | 售卖功能可用 |
| D9 | 1 | 条码追溯接口（完整流转时间线）；前端追溯查询页面（条码输入 + 时间线展示） | 追溯功能可用 |
| D10 | 1 | 师傅领用记录查询接口（按师傅 + 时间筛选）；前端师傅记录页面 | 师傅记录查询可用 |
| D11 | 1 | 操作日志自动记录机制（AOP 或手动埋点）；日志查询接口；前端日志页面 | 操作日志功能可用 |
| D12 | 1 | H5 端页面开发：登录、首页导航、入库、领用、归还、售卖操作页 | H5 端核心页面 |
| D13 | 1 | H5 扫码集成：QuaggaJS 摄像头调用、一维码识别、扫码回调处理 | H5 扫码功能可用 |
| D14 | 1 | H5 端查询页面（库存查询、追溯、师傅记录）；移动端响应式适配与交互优化 | H5 端完整可用 |
| D15 | 1 | PC + H5 双端联调；数据一致性验证；核心流程端到端测试；Bug 修复 | 联调通过、主要 Bug 修复 |
| D16 | 1 | 边界场景测试（重复入库、库存不足、批量操作、并发等）；性能验证；最终 Bug 修复 | 测试通过 |
| D17 | 1 | 项目打包（后端 JAR、前端 dist）；Nginx 配置；Linux / Windows 部署验证；服务自启动配置 | 可部署运行的完整包 |
| D18 | 1 | 编写部署文档、使用手册、运维说明；最终验收验证；交付 | 完整交付物 |

### 7.3 里程碑节点

| 里程碑 | 预计完成 | 验证标准 |
|--------|---------|---------|
| M1：基础可用 | D3 | 登录成功、师傅/分类管理页面正常 |
| M2：核心流转闭环 | D8 | 入库 → 领用 → 归还 → 售卖全流程跑通 |
| M3：查询追溯完整 | D11 | 库存查询、条码追溯、操作日志均可使用 |
| M4：H5 端完整 | D14 | H5 端全功能可用，扫码正常 |
| M5：测试通过 | D16 | 全功能自测通过，无阻塞性 Bug |
| M6：交付上线 | D18 | 部署完成，文档齐全，验收通过 |

---

## 8. 测试方案

### 8.1 测试策略

| 测试类型 | 范围 | 工具 |
|---------|------|------|
| 单元测试 | Service 层核心业务逻辑 | JUnit 5 + Mockito |
| 接口测试 | 全部 REST API | Postman / Apifox |
| 功能测试 | PC 端 + H5 端全部业务功能 | 手动测试 + 浏览器 DevTools |
| 兼容性测试 | PC 主流浏览器 + 移动端浏览器 | Chrome、Edge、Firefox、iOS Safari、Android Chrome |
| 数据一致性测试 | PC 与 H5 双端数据同步 | 双端同时操作验证 |
| 边界测试 | 异常场景、极端输入 | 手动构造边界条件 |

### 8.2 核心测试用例

#### 8.2.1 配件入库测试

| 编号 | 测试场景 | 预期结果 |
|------|---------|---------|
| T-IN-01 | 正常扫码入库（新条码） | 配件信息写入，总库存 +1，生成入库记录 |
| T-IN-02 | 手动输入条码入库 | 同上，功能一致 |
| T-IN-03 | 重复条码入库 | 系统提示"该条码已入库"，拒绝重复入库 |
| T-IN-04 | 入库必填项为空 | 前端校验提示，不允许提交 |
| T-IN-05 | 入库后查询库存 | 库存列表中可见新入库配件 |

#### 8.2.2 配件领用测试

| 编号 | 测试场景 | 预期结果 |
|------|---------|---------|
| T-OUT-01 | 正常扫码领用（选择师傅） | 总库存 -1，师傅库存 +1，生成领用记录 |
| T-OUT-02 | 批量扫码领用（3 个配件） | 3 个配件一次性转移，库存正确，3 条记录 |
| T-OUT-03 | 总库存为 0 时领用 | 提示"库存不足"，拒绝操作 |
| T-OUT-04 | 未选择师傅时提交 | 前端校验提示 |
| T-OUT-05 | 领用后查询师傅库存 | 师傅名下可见领用配件 |

#### 8.2.3 配件归还测试

| 编号 | 测试场景 | 预期结果 |
|------|---------|---------|
| T-RTN-01 | 正常扫码归还 | 师傅库存 -1，总库存 +1，生成归还记录 |
| T-RTN-02 | 批量归还 | 多个配件一次性归还，库存正确 |
| T-RTN-03 | 归还不属于该师傅的配件 | 提示"该师傅名下无此配件"，拒绝操作 |
| T-RTN-04 | 归还后查询总库存 | 库存数量正确回增 |

#### 8.2.4 配件售卖测试

| 编号 | 测试场景 | 预期结果 |
|------|---------|---------|
| T-SELL-01 | 正常售卖（填写客户信息） | 总库存 -1，生成售卖记录（含客户信息） |
| T-SELL-02 | 售卖（不填客户信息） | 总库存 -1，售卖记录客户字段为空，操作成功 |
| T-SELL-03 | 总库存为 0 时售卖 | 提示"库存不足"，拒绝操作 |

#### 8.2.5 查询追溯测试

| 编号 | 测试场景 | 预期结果 |
|------|---------|---------|
| T-QRY-01 | 按条码追溯 | 展示该配件完整流转时间线（入库 → 领用 → 归还 → 售卖） |
| T-QRY-02 | 按师傅查询记录 | 展示该师傅全部领用/归还记录 |
| T-QRY-03 | 总库存按分类筛选 | 仅显示选定分类的配件 |
| T-QRY-04 | 多条件组合查询 | 按时间 + 类型 + 师傅组合筛选，结果正确 |

#### 8.2.6 双端同步测试

| 编号 | 测试场景 | 预期结果 |
|------|---------|---------|
| T-SYNC-01 | PC 端入库后，H5 端查询库存 | H5 端即时显示新入库配件 |
| T-SYNC-02 | H5 端领用后，PC 端查询师傅库存 | PC 端即时显示新领用配件 |
| T-SYNC-03 | PC 端和 H5 端同时操作不同配件 | 两个操作均成功，库存各自正确 |

#### 8.2.7 异常与边界测试

| 编号 | 测试场景 | 预期结果 |
|------|---------|---------|
| T-ERR-01 | Token 过期后访问接口 | 返回 401，前端跳转登录页 |
| T-ERR-02 | 输入超长字符串 | 前后端均有长度校验 |
| T-ERR-03 | 网络断开时操作 | 前端提示网络异常 |
| T-ERR-04 | 删除有名下配件的师傅 | 系统拒绝删除，提示原因 |
| T-ERR-05 | 服务重启后数据验证 | 数据持久化，重启后所有数据完整 |

### 8.3 测试执行流程

```
1. 开发自测（每个功能开发完成后即时验证）
   ↓
2. 模块集成测试（Phase 2/3 完成后，端到端流程验证）
   ↓
3. 双端联调测试（Phase 5，PC + H5 数据一致性）
   ↓
4. 全量回归测试（Phase 5，全部功能回归验证）
   ↓
5. 边界与异常测试（Phase 5，异常场景覆盖）
   ↓
6. 部署验证（Phase 6，生产环境部署后全流程验证）
```

---

## 9. 打包部署流程

### 9.1 项目打包

#### 9.1.1 后端打包

```bash
# 进入后端项目根目录
cd accessory-backend

# Maven 打包（跳过测试加速打包）
mvn clean package -DskipTests

# 产出物
# target/accessory-backend.jar
```

#### 9.1.2 前端打包（PC 端）

```bash
# 进入 PC 端项目根目录
cd accessory-pc

# 安装依赖
npm install

# 生产环境构建
npm run build

# 产出物：dist/ 目录
```

#### 9.1.3 前端打包（H5 端）

```bash
# 进入 H5 端项目根目录
cd accessory-h5

# 安装依赖
npm install

# 生产环境构建
npm run build

# 产出物：dist/ 目录
```

### 9.2 服务器环境要求

| 组件 | 版本要求 | 说明 |
|------|---------|------|
| JDK | 17+ | 运行 Spring Boot JAR |
| MySQL | 8.0+ | 数据库 |
| Nginx | 1.24+ | 反向代理与静态资源托管 |

### 9.3 部署步骤（Linux）

#### Step 1：安装基础环境

```bash
# 安装 JDK 17
sudo apt update
sudo apt install openjdk-17-jdk -y
java -version

# 安装 MySQL 8.0
sudo apt install mysql-server -y
sudo mysql_secure_installation

# 安装 Nginx
sudo apt install nginx -y
sudo systemctl start nginx
sudo systemctl enable nginx
```

#### Step 2：初始化数据库

```bash
# 登录 MySQL
mysql -u root -p

# 创建数据库
CREATE DATABASE accessory_db DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 创建专用用户（可选，建议）
CREATE USER 'accessory'@'localhost' IDENTIFIED BY 'YourStrongPassword';
GRANT ALL PRIVILEGES ON accessory_db.* TO 'accessory'@'localhost';
FLUSH PRIVILEGES;

# 导入初始化 SQL
mysql -u accessory -p accessory_db < sql/init.sql
```

#### Step 3：部署后端服务

```bash
# 创建部署目录
sudo mkdir -p /opt/accessory/backend
sudo mkdir -p /opt/accessory/logs

# 拷贝 JAR 包
sudo cp target/accessory-backend.jar /opt/accessory/backend/

# 修改配置文件（见 9.5 配置说明）
# application.yml 放置于 JAR 同目录或外部指定路径
```

#### Step 4：部署前端文件

```bash
# 创建前端部署目录
sudo mkdir -p /opt/accessory/pc
sudo mkdir -p /opt/accessory/h5

# 拷贝前端构建产物
sudo cp -r accessory-pc/dist/* /opt/accessory/pc/
sudo cp -r accessory-h5/dist/* /opt/accessory/h5/
```

#### Step 5：配置 Nginx

```nginx
server {
    listen       80;
    server_name  your-domain.com;  # 或 IP 地址

    # PC 端
    location / {
        root /opt/accessory/pc;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # H5 端
    location /h5 {
        alias /opt/accessory/h5;
        index index.html;
        try_files $uri $uri/ /h5/index.html;
    }

    # API 代理
    location /api/ {
        proxy_pass http://127.0.0.1:8080/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

```bash
# 检查 Nginx 配置
sudo nginx -t

# 重载 Nginx
sudo systemctl reload nginx
```

#### Step 6：启动后端服务

```bash
# 前台启动（测试验证）
java -jar /opt/accessory/backend/accessory-backend.jar \
  --spring.config.location=/opt/accessory/backend/application.yml

# 后台启动
nohup java -jar /opt/accessory/backend/accessory-backend.jar \
  --spring.config.location=/opt/accessory/backend/application.yml \
  > /opt/accessory/logs/accessory.log 2>&1 &
```

#### Step 7：配置开机自启（systemd）

创建服务文件 `/etc/systemd/system/accessory.service`：

```ini
[Unit]
Description=Accessory Management System
After=network.target mysql.service

[Service]
Type=simple
User=root
WorkingDirectory=/opt/accessory/backend
ExecStart=/usr/bin/java -jar /opt/accessory/backend/accessory-backend.jar --spring.config.location=/opt/accessory/backend/application.yml
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

```bash
# 加载配置、设置自启、启动服务
sudo systemctl daemon-reload
sudo systemctl enable accessory.service
sudo systemctl start accessory.service

# 查看状态
sudo systemctl status accessory.service
```

### 9.4 部署步骤（Windows）

```bash
# 1. 安装 JDK 17、MySQL 8.0、Nginx（官方安装包）
# 2. 初始化数据库（同 Linux Step 2）
# 3. 部署后端 JAR 到目标目录
# 4. 部署前端 dist 到目标目录
# 5. 配置 Nginx（nginx.conf，配置内容同 Linux）
# 6. 启动后端
java -jar accessory-backend.jar --spring.config.location=application.yml

# 7. 开机自启（使用 NSSM 注册为 Windows 服务）
nssm install AccessoryService java "-jar" "C:\accessory\backend\accessory-backend.jar" "--spring.config.location=C:\accessory\backend\application.yml"
nssm start AccessoryService
```

### 9.5 配置文件说明

后端 `application.yml` 关键配置项：

```yaml
server:
  port: 8080                    # 服务端口

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/accessory_db?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai
    username: accessory          # 数据库用户名
    password: YourStrongPassword # 数据库密码
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  mapper-locations: classpath:mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true

jwt:
  secret: your-jwt-secret-key   # JWT 密钥（建议修改为随机长字符串）
  expiration: 86400000           # Token 过期时间（毫秒），默认 24 小时
```

### 9.6 部署验证清单

| 验证项 | 验证方法 | 预期结果 |
|--------|---------|---------|
| 后端服务启动 | `curl http://localhost:8080/api/auth/info` | 返回 401（未登录，但服务正常响应） |
| PC 端访问 | 浏览器打开 `http://服务器IP/` | 显示登录页面 |
| H5 端访问 | 手机浏览器打开 `http://服务器IP/h5` | 显示登录页面 |
| 登录功能 | 输入管理员账号密码登录 | 登录成功，跳转首页 |
| 核心流程 | 入库 → 领用 → 归还 → 售卖全流程 | 全流程正常 |
| 服务自启 | 重启服务器 | accessory 服务自动启动，双端可正常访问 |

---

## 10. 上线验收标准

### 10.1 功能验收

| 验收项 | 验收标准 | 通过条件 |
|--------|---------|---------|
| 配件入库 | 扫码/手动输入条码，录入配件信息，写入库存 | 入库后库存列表可见，操作记录生成 |
| 配件领用 | 扫码选择师傅，支持批量领用 | 总库存扣减，师傅库存增加，记录生成 |
| 配件归还 | 扫码选择师傅，支持批量归还 | 师傅库存扣减，总库存增加，记录生成 |
| 配件售卖 | 扫码扣减库存，选填客户信息 | 总库存扣减，售卖记录生成，客户信息正确 |
| 师傅管理 | 增删改查师傅信息 | CRUD 操作正常，删除校验有效 |
| 配件分类 | 增删改查分类 | CRUD 操作正常，入库时可选择 |
| 总库存查询 | 列表展示、分类筛选、关键词搜索 | 查询结果正确，分页正常 |
| 师傅库存查询 | 按师傅查看名下配件 | 展示正确，领用记录完整 |
| 条码追溯 | 按条码查询全流程流转时间线 | 时间线完整，每步操作可追溯 |
| 操作日志 | 全操作记录，支持多条件筛选 | 日志完整，筛选功能正常 |
| H5 扫码 | 手机摄像头扫描一维条码 | 扫码识别准确，自动填入条码 |
| 双端数据同步 | PC 与 H5 操作后数据即时一致 | 任一端操作，另一端即时可见 |

### 10.2 交付物验收

| 交付物 | 说明 |
|--------|------|
| 完整源码 | 后端 Java 工程 + PC 前端工程 + H5 前端工程 |
| 数据库 SQL 脚本 | 建表脚本 `init.sql`，含初始数据（管理员账号） |
| 可运行 JAR 包 | 后端打包产物 `accessory-backend.jar` |
| 前端构建产物 | PC 端 `dist/`、H5 端 `dist/` |
| 部署文档 | 详细的环境搭建与部署步骤文档 |
| 使用手册 | 管理员操作指南（含截图说明） |
| 运维说明 | 日常运维、备份、故障排查指南 |

### 10.3 非功能验收

| 验收项 | 标准 |
|--------|------|
| 浏览器兼容 | PC：Chrome、Edge、Firefox 正常访问 |
| 移动端兼容 | iOS Safari、Android Chrome 正常访问，扫码正常 |
| 数据持久化 | 服务重启后所有数据完整无丢失 |
| 服务自启 | 服务器重启后服务自动启动 |
| 配置可改 | 数据库连接、端口号等可通过配置文件修改 |

---

## 11. 使用与运维说明

### 11.1 使用说明

#### 11.1.1 系统登录

- PC 端：浏览器打开系统地址，输入管理员账号密码登录。
- H5 端：手机浏览器打开 H5 地址，输入相同账号密码登录。

#### 11.1.2 日常操作流程

**配件入库**
1. 进入「配件入库」页面。
2. 使用手机摄像头扫描配件一维条码（或手动输入条码）。
3. 填写配件名称、规格型号、选择分类、填写单位等信息。
4. 点击「确认入库」，系统自动写入库存。

**配件领用**
1. 进入「配件领用」页面。
2. 扫码或输入配件条码，系统自动加载配件信息。
3. 选择领用师傅。
4. 如需批量领用，继续扫码添加配件到待领用列表。
5. 确认无误后点击「提交领用」。

**配件归还**
1. 进入「配件归还」页面。
2. 扫码或输入配件条码，系统自动匹配当前持有师傅。
3. 确认归还信息，支持批量操作。
4. 点击「确认归还」。

**配件售卖**
1. 进入「配件售卖」页面。
2. 扫码或输入配件条码，系统加载配件信息。
3. 选填客户名称、联系电话。
4. 点击「确认售卖」。

**查询追溯**
1. 进入「库存查询」查看总库存或按师傅筛选。
2. 进入「条码追溯」输入条码查看完整流转时间线。
3. 进入「操作日志」按条件筛选查看全部操作记录。

### 11.2 运维说明

#### 11.2.1 服务管理

```bash
# 查看服务状态
sudo systemctl status accessory.service

# 启动服务
sudo systemctl start accessory.service

# 停止服务
sudo systemctl stop accessory.service

# 重启服务
sudo systemctl restart accessory.service

# 查看实时日志
sudo journalctl -u accessory.service -f

# 查看历史日志（最近 100 行）
sudo journalctl -u accessory.service -n 100
```

#### 11.2.2 数据库备份

```bash
# 手动备份（Linux）
mysqldump -u accessory -p accessory_db > /opt/accessory/backup/backup_$(date +%Y%m%d_%H%M%S).sql

# 定时备份（配置 crontab）
crontab -e
# 添加以下行：每天凌晨 2 点自动备份
0 2 * * * mysqldump -u accessory -p'YourPassword' accessory_db > /opt/accessory/backup/backup_$(date +\%Y\%m\%d).sql
```

#### 11.2.3 日志管理

- 应用日志路径：`/opt/accessory/logs/accessory.log`
- 建议配置日志轮转（logrotate 或 Spring Boot 内置日志配置），避免日志文件过大。
- 日志级别可在 `application.yml` 中动态调整：

```yaml
logging:
  level:
    com.accessory: info  # 调试时可改为 debug
  file:
    name: /opt/accessory/logs/accessory.log
```

#### 11.2.4 常见故障排查

| 故障现象 | 可能原因 | 排查步骤 |
|---------|---------|---------|
| 网页无法访问 | Nginx 未启动或配置错误 | `sudo nginx -t` 检查配置，`sudo systemctl status nginx` |
| 接口返回 502 | 后端服务未启动 | `sudo systemctl status accessory.service` |
| 登录失败 | 数据库连接异常 | 检查 MySQL 是否运行，配置文件中数据库信息是否正确 |
| 扫码无法调用摄像头 | H5 端未使用 HTTPS | 摄像头 API 要求安全上下文（HTTPS 或 localhost） |
| 数据丢失 | 数据库未持久化 | 检查 MySQL 数据目录，确认未误删数据文件 |

#### 11.2.5 版本更新流程

```bash
# 1. 停止当前服务
sudo systemctl stop accessory.service

# 2. 备份数据库
mysqldump -u accessory -p accessory_db > /opt/accessory/backup/before_update_$(date +%Y%m%d).sql

# 3. 替换后端 JAR
sudo cp new-accessory-backend.jar /opt/accessory/backend/accessory-backend.jar

# 4. 替换前端文件
sudo rm -rf /opt/accessory/pc/*
sudo cp -r new-pc-dist/* /opt/accessory/pc/
sudo rm -rf /opt/accessory/h5/*
sudo cp -r new-h5-dist/* /opt/accessory/h5/

# 5. 执行数据库变更脚本（如有）
mysql -u accessory -p accessory_db < sql/upgrade_v1.1.sql

# 6. 重启服务
sudo systemctl start accessory.service

# 7. 验证
curl http://localhost:8080/api/auth/info
```

#### 11.2.6 安全建议

- 修改默认管理员密码，使用强密码。
- 修改 JWT 密钥为随机长字符串。
- 数据库使用专用用户，不使用 root。
- 生产环境建议配置 HTTPS（可通过 Let's Encrypt 免费获取证书）。
- 定期备份数据库，备份文件存储于独立位置。
- 限制服务器访问 IP（如仅内网访问，配置防火墙规则）。

---

## 附录

### A. 项目目录结构（建议）

```
accessory-system/
├── accessory-backend/              # 后端工程
│   ├── src/
│   │   ├── main/
│   │   │   ├── java/com/accessory/
│   │   │   │   ├── config/          # 配置类（JWT、MyBatis、CORS 等）
│   │   │   │   ├── controller/      # 控制器层
│   │   │   │   ├── service/         # 业务层
│   │   │   │   │   └── impl/        # 业务实现
│   │   │   │   ├── mapper/          # 数据层
│   │   │   │   ├── entity/          # 实体类
│   │   │   │   ├── dto/             # 数据传输对象
│   │   │   │   ├── vo/              # 视图对象
│   │   │   │   ├── enums/           # 枚举定义
│   │   │   │   ├── exception/       # 异常处理
│   │   │   │   ├── interceptor/     # 拦截器（JWT 鉴权）
│   │   │   │   ├── util/            # 工具类
│   │   │   │   └── AccessoryApplication.java
│   │   │   └── resources/
│   │   │       ├── mapper/          # MyBatis XML 映射
│   │   │       └── application.yml  # 配置文件
│   │   └── test/                    # 单元测试
│   ├── pom.xml
│   └── sql/
│       └── init.sql                 # 数据库初始化脚本
│
├── accessory-pc/                    # PC 前端工程
│   ├── src/
│   │   ├── api/                     # API 接口定义
│   │   ├── views/                   # 页面组件
│   │   ├── components/              # 公共组件
│   │   ├── router/                  # 路由配置
│   │   ├── utils/                   # 工具函数（请求封装等）
│   │   ├── assets/                  # 静态资源
│   │   ├── App.vue
│   │   └── main.js
│   ├── index.html
│   ├── vite.config.js
│   └── package.json
│
├── accessory-h5/                    # H5 前端工程
│   ├── src/
│   │   ├── api/                     # API 接口定义
│   │   ├── views/                   # 页面组件
│   │   ├── components/              # 公共组件（含扫码组件）
│   │   ├── router/                  # 路由配置
│   │   ├── utils/                   # 工具函数
│   │   ├── assets/                  # 静态资源
│   │   ├── App.vue
│   │   └── main.js
│   ├── index.html
│   ├── vite.config.js
│   └── package.json
│
└── docs/                            # 项目文档
    ├── 部署文档.md
    ├── 使用手册.md
    └── 运维说明.md
```

### B. 参考资料

- [Spring Boot 官方文档](https://spring.io/projects/spring-boot)
- [MyBatis-Plus 官方文档](https://baomidou.com/)
- [Vue 3 官方文档](https://vuejs.org/)
- [Element Plus 官方文档](https://element-plus.org/)
- [Vant 4 官方文档](https://vant-ui.github.io/vant/)
- [QuaggaJS 文档](https://serratus.github.io/quaggaJS/)

---

> **文档结束**  
> 本文档为配件流转管理系统开发计划的完整版本，涵盖从需求分析到部署运维的全流程。后续开发过程中如有需求变更或设计调整，应及时更新本文档以保持一致性。
